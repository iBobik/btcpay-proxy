{
  "name": "btcpay-proxy",
  "version": "0.0.1",
  "devDependencies": {
    "typescript": "5.8.3",
    "wrangler": "^4.18.0"
  },
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  }
}
